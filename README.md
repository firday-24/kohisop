# Kohisop Part 2

Welcome to the...

## Folder Structure

The workspace is at the src folder that contains three folders, where:

- `model`: the folder to define all objects that needed in every feature
- `processor`: the folder that contains business logic
- `Currency`: the folder to define object that needed in currency feature

